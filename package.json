{
  "name": "icecast-import",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon",
    "build": "next build && tsc --project tsconfig.server.json",
    "export": "next export",
    "start": "NODE_ENV=production node dist/index.js",
    "build:start": "npm run build && npm run start",
    "build:export": "npm run build && next export"
  },
  "engines": {
    "node": "14.17.0"
  },
  "dependencies": {
    "icecast-metadata-player": "1.3.2",
    "next-transpile-modules": "^8.0.0",
    "next": "^11.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@next/bundle-analyzer": "^10.0.7",
    "@types/node": "^14.14.27",
    "@types/react": "^17.0.1",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.3.4"
  }
}